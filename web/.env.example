# PostgreSQL — use Neon (https://neon.tech) for free tier Vercel deployment
DATABASE_URL="postgresql://user:password@host/dbname?sslmode=require"

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="set-a-32-byte-random-secret"
# Optional Auth.js v5 aliases (equivalent to NEXTAUTH_*):
# AUTH_URL="http://localhost:3000"
# AUTH_SECRET="set-a-32-byte-random-secret"

EMAIL_FROM="Interview Coach AI <no-reply@example.com>"
EMAIL_SERVER_HOST=""
EMAIL_SERVER_PORT=""
EMAIL_SERVER_USER=""
EMAIL_SERVER_PASSWORD=""

OTP_SALT=""
GUEST_COOKIE_SECRET=""
SEED_SECRET=""

# Deployment notes:
# 1. Set DATABASE_URL to your Neon PostgreSQL connection string
# 2. Run: npx prisma migrate deploy
# 3. Deploy to Vercel — no Python service needed
